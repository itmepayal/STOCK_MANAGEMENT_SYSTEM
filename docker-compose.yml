version: "3.8"

services:
  api:
    build: .
    container_name: stock-api
    ports:
      - "8000:8000"
    depends_on:
      - redis
    env_file:
      - .env

  redis:
    image: redis:7
    container_name: stock-redis
    ports:
      - "6379:6379"
